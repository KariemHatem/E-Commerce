import { FlowbiteInit } from './flowbite-init';

describe('FlowbiteInit', () => {
  it('should create an instance', () => {
    const directive = new FlowbiteInit();
    expect(directive).toBeTruthy();
  });
});
